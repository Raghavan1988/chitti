# Command-line entry point for Odysseus (stub today; completed on day 5).
"""Day 4 — cli: the command-line front door (a stub until day 5).

Concept: `python3 -m odysseus` should run the agent from a terminal. Day 5
fills this in with argument parsing, --resume, and a live event trace; today it
is a placeholder so the package entry point exists and imports cleanly.
"""


def main():
    """Print a placeholder message; the real CLI arrives on day 5."""
    print("Odysseus CLI arrives on day 5. For now, drive it via the Harness class:")
    print("    from odysseus import Harness")
    print('    Harness(workdir=".").run("your task")')
